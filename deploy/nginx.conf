# isnad reverse proxy — HTTP (SSL-ready)
# When domain + certs are ready, uncomment the SSL server block below.

upstream api {
    server api:8420;
    keepalive 16;
}

upstream web {
    server web:3000;
    keepalive 16;
}

server {
    listen 80;
    server_name _;

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 5;
    gzip_min_length 1024;
    gzip_types
        text/plain
        text/css
        text/xml
        application/json
        application/javascript
        application/xml
        application/xml+rss
        image/svg+xml;

    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_connect_timeout 5s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;
    proxy_buffering on;

    # Uncomment for ACME / Let's Encrypt
    # location /.well-known/acme-challenge/ {
    #     root /var/www/certbot;
    # }

    location /api/v1/ {
        proxy_pass http://api/api/v1/;
    }

    location /_next/static/ {
        proxy_pass http://web;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    location / {
        proxy_pass http://web;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# ── SSL server block (uncomment when certs are ready) ──
# server {
#     listen 443 ssl http2;
#     server_name YOUR_DOMAIN;
#
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#
#     gzip on;
#     gzip_vary on;
#     gzip_proxied any;
#     gzip_comp_level 5;
#     gzip_min_length 1024;
#     gzip_types
#         text/plain
#         text/css
#         text/xml
#         application/json
#         application/javascript
#         application/xml
#         application/xml+rss
#         image/svg+xml;
#
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_connect_timeout 5s;
#     proxy_send_timeout 60s;
#     proxy_read_timeout 60s;
#     proxy_buffering on;
#
#     location /api/v1/ {
#         proxy_pass http://api/api/v1/;
#     }
#
#     location /_next/static/ {
#         proxy_pass http://web;
#         add_header Cache-Control "public, max-age=31536000, immutable";
#     }
#
#     location / {
#         proxy_pass http://web;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }
